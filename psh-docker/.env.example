# Compose host port -> nginx:9000
PSH_PROXY_HTTP_PORT=9000

# Passed through to FastAPI app container
PSH_JWT_SECRET=change-this-in-production
PSH_DEFAULT_USERNAME=admin
PSH_DEFAULT_PASSWORD=admin123!
PSH_JWT_EXPIRE_MINUTES=480
